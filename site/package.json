{
  "name": "zenodo-rdm-site",
  "version": "0.0.0",
  "private": true,
  "description": "Lint workspace for zenodo-rdm site assets.",
  "scripts": {
    "lint": "eslint --ext .js .",
    "lint:fix": "eslint --ext .js --fix ."
  },
  "devDependencies": {
    "@inveniosoftware/eslint-config-invenio": "^2.0.0",
    "eslint": "^8"
  },
  "engines": {
    "node": ">=18"
  }
}
